eureka:
  instance:
    appname: eureka-customer
  client:
    service-url:
      defaultZone: http://localhost:8091/eureka/
    fetch-registry: true
#必须配置，否则fallBack不生效
feign:
  hystrix:
    enabled: true